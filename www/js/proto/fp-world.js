/**
 * FpWorld — first-person grid model (placeholder, implemented by the prototype work)
 */
var FpWorld = (function() { return {}; })();
if (typeof module !== "undefined" && module.exports) { module.exports = FpWorld; }
